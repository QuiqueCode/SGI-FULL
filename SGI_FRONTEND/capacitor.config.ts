import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Sistema Gestor de incidencias',
  webDir: 'dist'
};

export default config;
